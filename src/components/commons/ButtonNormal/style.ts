import styled from "styled-components"
import * as globalStyles from "../../../styles/global"

export const SCButtonNormal = styled.button`
    border: none;
    color: ${globalStyles.white};
    background-color: ${globalStyles.mainColor};
    padding: 8px 24px;
    font-size: 16px;
    border-radius: 8px;
    font-weight: 500;
    cursor: pointer;
`